{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "git add -A && git commit -m \"auto: claude changes\" 2>/dev/null || true; git push origin main 2>/dev/null || true",
            "statusMessage": "Committing and pushing to GitHub..."
          }
        ]
      }
    ]
  }
}
